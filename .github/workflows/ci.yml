name: CI/CD Pipeline

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build-test:
    name: Build & Test Services
    runs-on: ubuntu-latest
    strategy:
      matrix:
        service: [workflow-service] # เพิ่ม service อื่น ๆ ได้
    steps:
      # ----------------------------
      # Checkout source code
      - name: Checkout source
        uses: actions/checkout@v4

      # ----------------------------
      # Set up Go
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.24.2"

      # ----------------------------
      # Cache Go modules
      - name: Cache Go modules
        uses: actions/cache@v3
        with:
          path: ~/go/pkg/mod
          key: ${{ runner.os }}-go-${{ hashFiles('services/${{ matrix.service }}/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-

      # ----------------------------
      # Download dependencies
      - name: Download Go dependencies
        working-directory: services/workflow-service
        run: go mod download

      # ----------------------------
      # Run tests
      - name: Run tests
        working-directory: ./services/workflow-service
        run: go test ./... -v

      # ----------------------------
      # Build binary
      - name: Build binary
        working-directory: ./services/workflow-service
        run: go build -v ./cmd/main.go

  docker:
    name: Docker Build & Push Services
    needs: build-test
    runs-on: ubuntu-latest
    strategy:
      matrix:
        service: [workflow-service] # เพิ่ม service อื่น ๆ ได้
    steps:
      # ----------------------------
      # Checkout source code
      - name: Checkout source
        uses: actions/checkout@v4

      # ----------------------------
      # Docker login
      - name: Login Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # ----------------------------
      # Build Docker image
      - name: Build Docker Image
        working-directory: ./services/${{ matrix.service }}
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/${{ matrix.service }}:latest .
          docker tag ${{ secrets.DOCKER_USERNAME }}/${{ matrix.service }}:latest \
                     ${{ secrets.DOCKER_USERNAME }}/${{ matrix.service }}:${{ github.sha }}

      # ----------------------------
      # Push Docker image
      - name: Push Docker Image
        run: |
          docker push ${{ secrets.DOCKER_USERNAME }}/${{ matrix.service }}:latest
          docker push ${{ secrets.DOCKER_USERNAME }}/${{ matrix.service }}:${{ github.sha }}
