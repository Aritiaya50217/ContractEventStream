name: CI/CD Pipeline

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  workflow_dispatch:

jobs:
  build-test:
    name: Build & Test Services
    runs-on: ubuntu-latest

    strategy:
      matrix:
        service: [workflow-service]

    steps:
      # ----------------------------
      # Checkout
      - name: Checkout source
        uses: actions/checkout@v4

      # ----------------------------
      # Try Go 1.24 first
      - name: Set up Go 1.24 (primary)
        id: go124
        uses: actions/setup-go@v5
        continue-on-error: true
        with:
          go-version: "1.24"

      # ----------------------------
      # Fallback to Go 1.22 if 1.24 fails
      - name: Set up Go 1.22 (fallback)
        if: steps.go124.outcome == 'failure'
        uses: actions/setup-go@v5
        with:
          go-version: "1.22.x"

      # ----------------------------
      # Verify Go version
      - name: Check Go version
        run: go version

      # ----------------------------
      # Cache Go modules
      - name: Cache Go modules
        uses: actions/cache@v3
        with:
          path: ~/go/pkg/mod
          key: ${{ runner.os }}-go-${{ hashFiles(format('services/{0}/go.sum', matrix.service)) }}
          restore-keys: |
            ${{ runner.os }}-go-

      # ----------------------------
      # Download dependencies
      - name: Download Go dependencies
        working-directory: services/${{ matrix.service }}
        run: go mod download -C services/workflow-service

      # ----------------------------
      # Run tests
      - name: Run tests
        working-directory: services/${{ matrix.service }}
        run: go test ./... -v -C services/workflow-service

      # ----------------------------
      # Build binary
      - name: Build binary
        working-directory: services/${{ matrix.service }}
        run: go build -v ./cmd/main.go -C services/workflow-service

  docker:
    name: Docker Build & Push Services
    needs: build-test
    runs-on: ubuntu-latest

    strategy:
      matrix:
        service: [workflow-service]

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Login Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build Docker Image
        working-directory: services/${{ matrix.service }}
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/${{ matrix.service }}:latest .
          docker tag ${{ secrets.DOCKER_USERNAME }}/${{ matrix.service }}:latest \
                     ${{ secrets.DOCKER_USERNAME }}/${{ matrix.service }}:${{ github.sha }}

      - name: Push Docker Image
        run: |
          docker push ${{ secrets.DOCKER_USERNAME }}/${{ matrix.service }}:latest
          docker push ${{ secrets.DOCKER_USERNAME }}/${{ matrix.service }}:${{ github.sha }}
